
// import { useEffect } from 'react';
// import { useTheme } from '../context/themeContext';

export const useCSSVariables = () => {
    // const { theme } = useTheme();

    // useEffect(() => {
    //     const root = document.documentElement;

    //     // Colors
    //     root.style.setProperty('--color-primary', theme.colors.primary);
    //     root.style.setProperty('--color-background', theme.colors.background);
    //     root.style.setProperty('--color-text', theme.colors.text);

    //     // Typography
    //     root.style.setProperty('--heading-size', `${theme.typography.heading.size}px`);
    //     root.style.setProperty('--button-size', `${theme.typography.button.size}px`);

    //     // Spacing
    //     root.style.setProperty('--header-height', `${theme.spacing.headerHeight}px`);
    //     root.style.setProperty('--button-width', `${theme.spacing.buttonWidth}px`);
    //     root.style.setProperty('--button-height', `${theme.spacing.buttonHeight}px`);
    //     root.style.setProperty('--button-radius', `${theme.border.buttonRadius}px`);
    //     root.style.setProperty('--button-border', `${theme.border.buttonBorder}px`);
    // }, [theme]);
};